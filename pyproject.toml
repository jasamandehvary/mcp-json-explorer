[project]
name = "mcp-json-explorer"
version = "0.1.0"
description = "An MCP server for querying, searching, flattening, diffing, and validating JSON — no jq syntax required."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.28.1",
]
